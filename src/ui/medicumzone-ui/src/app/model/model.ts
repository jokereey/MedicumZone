export interface NavigatorItem {
  header: string,
  description: string,
  order: number
}
export interface CovidStats{
  "deathsAtAll": string,
  "deathsToday": string,
  "infectedToday": string,
  "infectedAtAll": string,
  "recovered": string
}
